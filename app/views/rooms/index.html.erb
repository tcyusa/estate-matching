<h1>メッセージ一覧</h1>

<table class="table">
    <% @entries.each do |e| %>
        <tr>
            <% @room = Room.find(e.room_id) %>
            <td><%= link_to @room.name, room_path(@room) %>からのメッセージ</td>
        </tr>
    <% end %>
</table>
